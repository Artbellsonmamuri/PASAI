{
  "name": "pasai",
  "version": "1.0.0",
  "scripts": {
    "start": "http-server src -p $PORT -a 0.0.0.0",
    "dev": "http-server src -p 8080 -a localhost"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
